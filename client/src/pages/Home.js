import React from "react";
import ContentContainer from "../layouts/ContentContainer";

const Home = ({ isAuth }) => {
  return <ContentContainer isAuth={isAuth}>Home</ContentContainer>;
};

export default Home;
